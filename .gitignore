# Local skill install mirrors created by `npx skills add` — never commit these.
# The canonical source is the top-level <skill-name>/ directories.
.agent/
.agents/
.claude/
.cline/
.cursor/
.trae/
.windsurf/
skills/

# Env / secrets
.env
.env.*
!.env.example

# OS / editor
.DS_Store
node_modules/

# Org ASC credentials (never commit)
org-credentials.env
*.p8
__pycache__/
